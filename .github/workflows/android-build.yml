name: Android CI

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        
    - name: Generate Platform Folders
      run: flutter create --platforms=android .
        
    - name: Inject Required Permissions
      run: |
        sed -i '/<manifest/a \    <uses-permission android:name="android.permission.INTERNET" />\n    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />\n    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />\n    <uses-permission android:name="android.permission.CAMERA" />' android/app/src/main/AndroidManifest.xml
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Set Custom Package Name (com.kainuwa.africa)
      run: flutter pub run change_app_package_name:main com.kainuwa.africa
      
    - name: Generate Custom App Icon
      run: flutter pub run flutter_launcher_icons
      
    - name: Inject Persistent Digital Signature
      run: |
        mkdir -p ~/.android
        cp kainuwa_debug.keystore ~/.android/debug.keystore
      
    - name: Build Google Play App Bundle (.aab)
      run: flutter build appbundle --release
      
    - name: Build Optimized APK (.apk)
      run: flutter build apk --release
        
    - name: Secure & Rename APK
      run: |
        APK_PATH=$(find build -type f -name "*.apk" | grep -v "test" | head -n 1)
        mv "$APK_PATH" Optimized-Release.apk
        
    - name: Upload App Bundle
      uses: actions/upload-artifact@v4
      with:
        name: PlayStore-Release-AAB
        path: build/app/outputs/bundle/release/app-release.aab
        
    - name: Upload Optimized APK
      uses: actions/upload-artifact@v4
      with:
        name: Direct-Install-APK
        path: Optimized-Release.apk
